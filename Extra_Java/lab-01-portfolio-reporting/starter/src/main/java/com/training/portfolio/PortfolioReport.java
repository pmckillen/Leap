package com.training.portfolio;

/**
 * Holds a simple collection of assets for one client's portfolio and
 * produces basic reports over that collection.
 *
 * Build this out so it can:
 *  - hold an arbitrary number of Asset objects
 *  - add a new asset
 *  - return the assets currently held, without letting a caller mutate
 *    the portfolio's internal collection through the returned value
 *  - report the total value of everything held
 *  - report the total value, grouped by asset type
 *  - report the number of assets, grouped by asset type
 *  - report the distinct set of asset types currently held
 *  - identify the single highest-value asset - and decide what should
 *    happen if the portfolio is empty
 *
 * Think about which of List, Map and Set from the collections framework
 * fits each piece of this.
 */
public class PortfolioReport {

}
